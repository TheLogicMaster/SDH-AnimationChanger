import { useEffect, FC } from "react";

export const UninstallAnimationPage: FC = () => {
  
  // Runs upon opening the page
  useEffect(() => {
    //
  }, []);

  return (
    <div>
      <h2
        style={{ fontWeight: "bold", fontSize: "1.5em", marginBottom: "0px" }}
      >
        TODO
      </h2>
    </div>
  );
};